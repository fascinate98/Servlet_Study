서블릿 - jsp : 서로 보완관계
mvc패턴 개발

서블릿의 장점을 가져와서 
쓰레드나 객체한개만만드러두고쓰거나 그러넉ㄴ ㅇㅇ다가능
jsp 실행될떄 서블릿으로 바뀜
jsp -> 서블릿 변환해주는애 : jsp컨버터

was: web application server == web serv + app serv
servlet container + jsp converter
                       -------------------- -> 얘가 서블릿으로 변환해주는애
다 와스안에 드러잇음.
최초요청일때 한번만 바뀌고 그때부터는 서블릿임

서블릿에서 공부한내용 다 jsp 

스크립트태그//액션태그//

jsp -> java -> class -> was-> servlet -> 나감


=-=
아무것도 입력안하고 전송: 
파트 세개는 감
벨류가 비어잇고 키는 잇음
마이네임 마이파일 이런거

널문자열은 객체가 있는거


<%= LocalDateTime.now() %> 저거 꺽쇠 안에 쓰면댐(세미콜론 ㄴㄴ)

<%! int member_v = 0; %> 선언문 태그

<%
	int local_v = 0;
	member_v += 10;
	local_v += 10;
%>
 여러줄

<%-- --%> 주석문 태그
<%@ %> 지시자 태그  // jsp-> 서블릿으로 바뀔때 지시하는거. 자바아님. 정해진속성쓰는거
<% %> 스크립트릿(수행문) 태그  //요청시마다 메모리할당
<%! %> 선언문(변수선언, 메서드정의) 태그  //메모리 한번만 할당
<%= %> 표현식 // html안에 쓸떄  , 세미콜론 붙히면 안댐. 출력할 식만 작성함.

이 밑에 세개 합쳐서 얘네만 자바쓸수잇음
스크립팅태그라고도 함
간단한 자바 코드 포함

jsp는 응답용으로 설계되어서 get/post나뉘지않음

jsp는 서블릿으로 변경될때 세션객체 사용하든안하든 자동생성됨
30분이상 서버한테 요청 안하면 삭제 
사용 안할거면 안한다고 머 속성주면 댐

<h1>쿼리 문자열 추출 (Get&amp;POST)</h1> 
특수문자 머시기
태그로 인식 안하고 추 ㄹ력해주는애 
걍 & << 이 기호 쓰고싶어서 쓴듯
&lt;   이거 < 이거래 특수문자를 대신해서 작성하는 코두 

기본으로 서블릿메서드라서 겟포스트 구분 안함 
포스트쓰면 인코딩해줘야해서 이프문넣엊무
	if(request.getMethod().equals("Post")){
		
		request.setCharacterEncoding("utf-8");
	}
이ㅓㄹㄴ거 다 수행문 안에다가 구현


request.getHeader("referer") 요청한 사ㅏㄻ
요청한 html에대한 정보를 추출해줌 
직접요청하면 꺼낼수잇는정보가 없어서 절대패스로 주는거랑 그런거 차이 기억하기
장단점이 이슴

